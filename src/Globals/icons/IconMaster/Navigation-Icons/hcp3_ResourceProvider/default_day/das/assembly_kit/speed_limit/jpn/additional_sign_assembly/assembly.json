{
  "type": "merging",
  "id": "default_day.das.merging.speedlimit.jpn.additional_sign_assembly",
  "dependencies": [
    "default_day.das.copying.speedlimit.jpn.duplicate_base_signs"
  ],
  "workingSets": [
    {
      "inputPath": "speed_limit/not_regulated/jpn",
      "outputPath": "speed_limit/not_regulated/jpn"
    }
  ],
  "filter": {
    "comment": "A file is used as input if it passes all listed filter parts individually"
  },
  "mergeSets": [
    {
      "outputNaming": {
        "param4": 15,
        "subMarket": "jpn"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_japan_work.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    },
    {
      "outputNaming": {
        "param4": 16,
        "subMarket": "jpn"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_japan_school.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    }
  ]
}
