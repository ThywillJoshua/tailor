{
  "type": "merging",
  "id": "default_day.das.merging.speedlimit.chn.additional_sign_assembly",
  "dependencies": [
    "default_day.das.copying.speedlimit.chn.duplicate_base_signs"
  ],
  "workingSets": [
    {
      "inputPath": "speed_limit/not_regulated/chn",
      "outputPath": "speed_limit/not_regulated/chn"
    }
  ],
  "filter": {
    "comment": "A file is used as input if it passes all listed filter parts individually",
    "blockWith": {
      "param2": {
        "comment": "discard signs which don't have additional signs",
        "elementOf": [251, 255]
      }
    }
  },
  "mergeSets": [
    {
      "outputNaming": {
        "param4": 15,
        "subMarket": "chn"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_china_work.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    },
    {
      "outputNaming": {
        "param4": 16,
        "subMarket": "chn"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_china_school.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    }
  ]
}
