{
  "type": "merging",
  "id": "default_day.das.merging.speedlimit.nar.can.overlay.night",
  "dependencies": [
    "default_day.das.merging.speedlimit.nar.can.basic.night_label"
  ],
  "workingSets": [
    {
      "inputPath": "speed_limit/not_regulated/nar/can",
      "outputPath": "speed_limit/not_regulated/nar/can"
    }
  ],
  "filter": {
    "comment": "A file is used as input if it passes all listed filter parts individually"
  },
  "mergeSets": [
    {
      "outputNaming": {
        "param3": 1
      },
      "merge": {
        "mode": "overlay",
        "file": "overlay_unit.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    }
  ]
}
