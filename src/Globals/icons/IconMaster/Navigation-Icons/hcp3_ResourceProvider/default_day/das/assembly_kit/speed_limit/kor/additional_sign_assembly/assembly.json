{
  "type": "merging",
  "id": "default_day.das.merging.speedlimit.kor.additional_sign_assembly",
  "dependencies": [
    "default_day.das.copying.speedlimit.kor.duplicate_base_signs"
  ],
  "workingSets": [
    {
      "inputPath": "speed_limit/not_regulated/kor",
      "outputPath": "speed_limit/not_regulated/kor"
    }
  ],
  "filter": {
    "comment": "A file is used as input if it passes all listed filter parts individually"
  },
  "mergeSets": [
    {
      "outputNaming": {
        "param4": 15,
        "subMarket": "kor"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_korea_work.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    },
    {
      "outputNaming": {
        "param4": 16,
        "subMarket": "kor"
      },
      "merge": {
        "mode": "overlay",
        "file": "sign_korea_school.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    }
  ]
}
