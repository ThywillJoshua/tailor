{
  "type": "merging",
  "id": "default_day.das.merging.speedlimit_recommendation.nar.can.exit",
  "dependencies": [],
  "workingSets": [
    {
      "inputPath": "",
      "outputPath": "speed_limit_recommendation/not_regulated/nar/can"
    }
  ],
  "filter": {
    "comment": "A file is used as input if it passes all listed filter parts individually",
    "list": {
      "mode": "blacklist",
      "files": ["exit_sign.svg"]
    }
  },
  "mergeSets": [
    {
      "outputNaming": {},
      "merge": {
        "mode": "overlay",
        "file": "exit_sign.svg",
        "anchors": {
          "comment": "Specify the two points with [left, center, right, top, bottom] or a number. SVGs are merged such that those points are in the same spot",
          "overlay": {
            "horizontal": "left",
            "vertical": "top"
          },
          "background": {
            "horizontal": "left",
            "vertical": "top"
          }
        }
      }
    }
  ]
}
