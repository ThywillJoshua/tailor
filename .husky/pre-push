#!/bin/bash
# Prevents force-pushing to master

echo "===\n>> Pre-push Hook: Checking branch name..."

BRANCH=`git rev-parse --abbrev-ref HEAD`
PROTECTED_BRANCHES="^(master|develop)"

if [[ "$BRANCH" =~ $PROTECTED_BRANCHES ]]
then
  echo "\n🚫 Cannot push to remote $BRANCH branch, please create your own branch and use PR." && exit 1
fi

# if [[ $1 != *"$BRANCH"* ]]
# then
#   echo "\n🚫 You must use (git push origin $BRANCH) \n" && exit 1
# fi

echo ">> Finish checking branch name \n==="

#!/bin/bash

exec < /dev/tty
echo "                                    "
response=""
while [[ ! "$response" =~ ^[yn]$ ]]; do
    read -p "Build Storybook? (y/n): " response
    echo "------------------------------------"
    if [[ ! "$response" =~ ^[yn]$ ]]; then
        echo "Please enter 'y' or 'n'."
        echo "------------------------------------"
    fi
done

if [ "$response" == "y" ]; then
    npm run build-storybook || git reset --hard HEAD
else
    echo "Skipped build-storybook."
    echo "------------------------------------"
fi

exec < /dev/tty
echo "                                    "
echo "************************************"
echo "                                    "
response=""
while [[ ! "$response" =~ ^[yn]$ ]]; do
    read -p "Build Storybook HUD? (y/n): " response
    echo "------------------------------------"
    if [[ ! "$response" =~ ^[yn]$ ]]; then
        echo "Please enter 'y' or 'n'."
        echo "------------------------------------"
    fi
done

if [ "$response" == "y" ]; then
    npm run build-storybook-hud || git reset --hard HEAD
else
    echo "Skipped build-storybook-hud."
    echo "------------------------------------"
fi

# Continue with other pre-commit tasks or commands

exit 0